<Mylayout>
    BoxLayout:
        orientation: 'vertical'
        size:root.width,root.height

        padding:50
        spacing:20
        Label:
            text:"Select the user Category"
            font_size:32
        GridLayout:
            cols:2
            Label:
                id:checkbox_label
                text:"Student"
                font_size:20
            CheckBox:
                on_active:root.check_box_click(*args)
            Label:
                id:checkbox_label
                text:"Disable"
                font_size:20
            CheckBox:
                on_active:root.check_box_click(*args)
            Label:
                id:checkbox_label
                text:"Old age"
                font_size:20
            CheckBox:
                on_active:root.check_box_click(*args)
        Label:
            id:checkbox_result
            text:'You have selected'
                